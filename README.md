# Workspace Meta

This repository stores shared VS Code workspace files and other lightweight workspace-level metadata for projects under `/Users/wuxinbo/Documents/Personal`.

Current files:

- `daypalette.code-workspace`: multi-root workspace for `day_palette` and `daypalette-docs`

Use relative paths inside workspace files so the repo can move with the sibling project folders under `Personal/`.